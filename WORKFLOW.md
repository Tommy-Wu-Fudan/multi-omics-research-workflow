# Research workflow

本文件是按任务读取的工作入口。开始时先写清本轮任务、输入位置、批准范围与交付位置；无需在一次任务中执行全部阶段。

## 阶段与交付合同

| 阶段 | 最小输入 | 核心工作 | 实际交付与验收 |
|---|---|---|---|
| 1. 研究问题 | 科学问题、已有证据、数据可用性 | 明确人群、暴露、结局、时间与 estimand；区分主分析和探索 | 研究合同能由另一人理解；未决项有影响范围。读 PB01。 |
| 2. 数据预处理 | 原数据、字典、样本与变量定义 | 检查 ID/合并关系、风险集、时间编码、单位、缺失与特征矩阵 | 派生数据、字典、样本流、QC、输入指纹。读 PB02。 |
| 3. 关联与组学 | 已确定的合同、QC 数据、特征字典 | 先做少量模型预检，核对 N/事件、尺度、诊断与检验家族，再执行批准的计算 | 完整结果表含模型身份、估计/CI、P/调整 P、不可估计与失败状态。读 PB03。 |
| 4. 补充证据 | 新问题、可用遗传或中介输入 | 先判断可估计性、工具/测量和识别假设，再决定适用方法 | 来源、尺度、诊断及解释边界；没有可用工具时如实记录。读 PB04。 |
| 5. 图表交付 | 指定结果包、展示要求 | 锁定内容，按稳定行键绘制和修订，分别检查数字、含义、视觉与可编辑对象 | 源文件、交付文件、source-data 指针和核查记录。读 PB05。 |
| 6. 写作与修订 | 指定稿件、结果包、文献和批注 | 论断回到来源；逐条处理意见；传播表号/术语变更，保护引用与修订对象 | 可审阅稿件、意见处置和来源台账，明确未决事项。读 PB06。 |

全部 Playbook 链接见 [playbooks/README.md](playbooks/README.md)。

## 两项横跨阶段的检查

**来源核对：**阶段 3 至 6 遇到数字、模型、版本或论断不一致时，调用 `research-result-trace`。结果区分 supported、mismatch、unresolved 和 out-of-scope；缺少来源不能靠猜测补齐。

**展示修订：**阶段 5 至 6 只需调整已有图表时，调用 `research-display-revision`。保留统计数值、模型和检验规则，并检查最终文件的实际点位、文本和版面。

## 新项目与已有项目

新项目复制 [模板](templates/research-project/README.md)，填写研究合同、变量字典和决策记录。先填写已有证据支持的内容，不把空字段理解为默认选择。

已有项目先阅读本项目文档与实际文件。目录和命名已稳定时继续沿用；用来源指针连接本工作流，不迁移原数据来适配模板。研究合同可以是已有的等价文档。

## 运行状态

将 `planned`、`running`、`completed`、`failed`、`not_run`、`not_estimable` 和 `not_applicable` 分开记录。状态必须有对应的运行证据；文件存在、脚本可解析和历史报告不能替代本次统计执行。

验证也分维度：输入完整性、统计/语义一致、程序结构、文件打开、视觉检查、目标应用编辑。只报告完成的维度。

## 将新的经验带回工作流

先在自己的研究中记录可定位的失败、纠正和验收证据；把稳定的工作顺序写成项目 Playbook。只有跨任务反复出现、输入/输出明确且已有工具不能直接覆盖的流程，才考虑单独做 Skill。用隔离的虚拟资料独立测试，包含应触发、应绕开、正确对照和不可完成用例；修正后再测试受影响范围。

公开分享前只提取可复用规则与虚拟例子。参与者记录、未公开结果、稿件批注、凭据及本机目录继续留在各自受控项目中。
